{
  "name": "superior-skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs && node scripts/validate-skills.mjs && node scripts/audit-endpoints.mjs",
    "sync:upstream": "node scripts/sync-upstream.mjs",
    "lint": "pnpm run validate",
    "check-types": "pnpm run validate",
    "contract:refresh": "node scripts/audit-endpoints.mjs --refresh"
  }
}
